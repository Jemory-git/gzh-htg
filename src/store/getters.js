export const user = state => state.user;

export const loginState = state => state.loginState;

export const voicePlaying = state => state.voicePlaying;

export const currentVoice = state => state.currentVoice;

export const likeList = state => state.likeList;

export const currentChild = state => state.currentChild;

export const currentChildIndex = state => state.currentChildIndex;

export const integral = state => state.integral;

export const resultTip = state => state.resultTip;

export const params = state => state.params;

export const video = state => state.video;

export const isLoading = state => state.isLoading;
